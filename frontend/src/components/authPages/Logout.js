import React from 'react';
import {useFormik} from "formik";

const Logout = () => {
    const formik  = useFormik({
        initialValues: {}
    })

    return (
        <div>

        </div>
    );
};

export default Logout;